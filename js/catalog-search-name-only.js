(()=>{const input=document.querySelector('#catalog-search'),grid=document.querySelector('#catalogGrid'),btn=document.querySelector('#catalog-search-btn');if(!input||!grid)return;const norm=s=>String(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLocaleLowerCase('pt-BR').trim();function filterByName(){const q=norm(input.value);grid.querySelectorAll('.old-card').forEach(card=>{const name=norm(card.querySelector('h3')?.textContent);card.style.display=!q||name.includes(q)?'':'none'});const visible=[...grid.querySelectorAll('.old-card')].some(card=>card.style.display!=='none');let empty=grid.querySelector('.name-search-empty');if(!visible&&q){if(!empty){empty=document.createElement('p');empty.className='empty-state name-search-empty';empty.textContent='Nenhum passeio encontrado com esse nome.';grid.appendChild(empty)}}else empty?.remove()}input.addEventListener('input',()=>setTimeout(filterByName,0));input.addEventListener('keydown',e=>{if(e.key==='Enter')setTimeout(filterByName,0)});btn?.addEventListener('click',()=>setTimeout(filterByName,0));new MutationObserver(()=>filterByName()).observe(grid,{childList:true});filterByName()})();