const {json,makeSession}=require('./_utils');
module.exports=async(req,res)=>{if(req.method!=='POST')return json(res,405,{error:'Método não permitido'});const body=typeof req.body==='string'?JSON.parse(req.body||'{}'):(req.body||{});const user=process.env.CARINE_ADMIN_USER||process.env.ADMIN_USER||'carine';const pass=process.env.CARINE_ADMIN_PASSWORD||process.env.ADMIN_PASSWORD;if(!pass)return json(res,503,{error:'Defina CARINE_ADMIN_PASSWORD (ou ADMIN_PASSWORD) no Vercel.'});if(body.user!==user||body.password!==pass)return json(res,401,{error:'Usuário ou senha incorretos'});const token=makeSession(user);json(res,200,{ok:true},{'Set-Cookie':`carine_admin=${encodeURIComponent(token)}; Path=/; HttpOnly; Secure; SameSite=Strict; Max-Age=43200`});};
